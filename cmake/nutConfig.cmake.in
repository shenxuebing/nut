@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# 查找依赖
if(@PLATFORM_LINUX@)
    find_dependency(Threads)
endif()

# 包含目标文件
include("${CMAKE_CURRENT_LIST_DIR}/nutTargets.cmake")

# 设置变量
set(NUT_VERSION @PROJECT_VERSION@)
set(NUT_FOUND TRUE)

# 检查所有组件是否可用
check_required_components(nut)

